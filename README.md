# REPO

# What is this? 
IT will be a django based search mechanism. RIght now the 'tdd_embedded_search.py' is the most interesting file right now. Run 'db_setup.py' first. NOTE: Loading the HuggingFace model takes a moment so be patient.  